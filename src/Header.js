import React from "react";

const Header = (props) => {
    return(
        <div>
            <h1>Hello Redux</h1>
            <h2>Add Fruit Name and Price</h2>
        </div>
    )
}

export default Header;